server:
  port: 8084

spring:
  application:
    name: NOTIFICATION-SERVICE
  ai:
    openai:
          api-key: "YOURKEY"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
